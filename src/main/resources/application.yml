server:
  port: 8081
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true
spring:
  application:
    name: PortfolioPageBuilder
  config:
    import: configserver:http://3.35.186.99:8888 #ec2 주소 config 포트
  profiles:
    active: dev

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: ERROR

mybatis:
  config-location: classpath:META-INF/mybatis/mybatis-config.xml
